import { AutoCleanDirective } from './auto-clean.directive';

describe('AutoCleanDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCleanDirective();
    expect(directive).toBeTruthy();
  });
});
